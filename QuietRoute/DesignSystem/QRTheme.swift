import SwiftUI

enum QRTheme {
    static let background = Color(red: 0.94, green: 0.98, blue: 0.97)
    static let card = Color.white
    static let primary = Color(red: 0.10, green: 0.44, blue: 0.46)
    static let accent = Color(red: 0.52, green: 0.78, blue: 0.70)
    static let slate = Color(red: 0.20, green: 0.28, blue: 0.31)
    static let muted = Color(red: 0.45, green: 0.53, blue: 0.56)

    static let surfaceShadow = Color.black.opacity(0.06)
}
